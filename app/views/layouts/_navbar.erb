<nav class="navbar">
  <div class="navbar-container">
    <div class="navbar-brand">
      Crypta
      <%= image_tag "fingerprint-logo.png", class: "brand-logo" %>
    </div>
    <div class="navbar-menu">
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <%= link_to "Админ панель", admin_cpanel_path, class: "navbar-item #{"active" if current_page?(admin_cpanel_path)}", id: "button-to-admin-panel", data: { controller: "navbar", action: "click->navbar#active" } %>
          <%= link_to "История", admin_history_path, class: "navbar-item #{"active" if current_page?(admin_history_path)}", id: "button-to-history" %>
        <% end %>

        <%= link_to "Мои данные", upanel_path, class: "navbar-item #{"active" if current_page?(upanel_path)}", id: "button-to-my-data" %>
        <%= link_to "Профиль", edit_user_registration_path, class: "navbar-item #{"active" if current_page?(edit_user_registration_path)}", id: "button-to-my-profile" %>
        <%= link_to "Справка", articles_path, class: "navbar-item #{"active" if current_page?(articles_path)}", id: "button-to-help" %>

        <%= link_to "Выйти", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Вы уверены, что хотите выйти?"}, class: "btn btn-danger" %>
      <% end %>
    </div>
  </div>
</nav>