<%= form_with model: @user, url: users_path do |f| %>
  <%= f.label :credentials %>
  <%= f.collection_select :credentials_ids, @credentials, :id, :key, multiple: true %>

  <%= f.submit 'Сохранить' %>
<% end %>