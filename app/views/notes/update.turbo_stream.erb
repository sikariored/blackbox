<%= turbo_stream.update("note-edit") do
 end %>

<%= turbo_stream.update("left-column") do
  render 'notes'
end %>

<%= turbo_stream.update("note-info") do
  render 'note_info'
 end %>

<%#= prepend_flash %>