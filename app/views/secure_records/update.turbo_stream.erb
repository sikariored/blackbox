<%= turbo_stream.update("record-edit") do
end %>

<%= turbo_stream.update("record-info") do
  render 'record_info'
end %>

<%= turbo_stream.update("left-column") do
  render 'records'
end %>

<%#= prepend_flash %>