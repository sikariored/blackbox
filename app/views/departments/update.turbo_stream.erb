<%= turbo_stream.update("department-edit") do
  render 'form_edit'
end %>

<%= turbo_stream.update("department-info") do
  render 'department_info'
end %>

<%= turbo_stream.update("left-column") do
  render 'departments'
end %>

<%= prepend_flash %>